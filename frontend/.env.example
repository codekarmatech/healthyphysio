# Frontend Environment Variables
# Copy this file to .env and update values as needed

# API URL - Used for backend API calls
# For local development: Leave empty (auto-detects based on hostname)
# For production: Set to your production API URL
# Example: REACT_APP_API_URL=https://api.physioway.com/api
REACT_APP_API_URL=

# WebSocket Host - Used for real-time features
# For local development: Leave empty (auto-detects based on hostname)
# For production: Set to your production WebSocket host
# Example: REACT_APP_WS_HOST=api.physioway.com
REACT_APP_WS_HOST=
